module github.com/coregx/relica

go 1.25.0

// ZERO DEPENDENCIES
// This module requires only the Go standard library.
// No require blocks — truly zero external dependencies.
// Integration tests and benchmarks have their own go.mod in test/ and benchmark/.
